import { ComingSoon } from './Tasks'
export default function Dashboard() {
  return <ComingSoon title="Dashboard" phase={6} description="Ops overview — pipeline value, active kiosks, tasks due today, posts this week, and global search." />
}
